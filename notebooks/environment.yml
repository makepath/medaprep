# install: mamba env create -f environment.yml
# update: mamba env update -f environment.yml

name: geolt-notebooks
channels:
  - conda-forge
dependencies:
  - notebook
  - geopandas
  - rasterio
  - rioxarray
  - ipykernel
  - matplotlib 
  - boto3
  - xarray
  - dask
  - pystac
  - shapely
  - pystac-client
  - s3fs
